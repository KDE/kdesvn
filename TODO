    - let call revision tree for single item from log-window (changed items)
    - add revision tree to context menu in konqueror
    - partial revision tree, eg. tree over a specific range only.
    - resolve-editor for conflicts